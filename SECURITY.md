# ZenUI Security

ZenUI is source-available. Because Luau is delivered to a client/execution environment, no technical method can guarantee that distributed source cannot be copied or modified.

Protection therefore relies on ownership terms, licensing, canonical Git history, official build fingerprints, runtime provenance metadata, and branding restrictions.
